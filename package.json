{
  "name": "wms-consultant-agent",
  "version": "1.0.0",
  "description": "WMS Implementation Consultant Web Agent",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build completed'",
    "postinstall": "cd client && npm install && npm run build && mkdir -p ../server/public && cp -r build/* ../server/public/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.10.0"
  }
}
